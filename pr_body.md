🚨 Severity: HIGH
💡 Vulnerability: お問い合わせAPI (contact.ts) におけるインメモリのレート制限機構に、DoS（サービス拒否）の脆弱性が存在しました。最大バケット数 (`MAX_BUCKETS`) に達した際、古いエントリを単純に削除していたため、攻撃者が大量の新規IPからアクセスすることで、制限されているIPの履歴を意図的に押し出し（evict）、レート制限を回避できる状態でした。
🎯 Impact: 攻撃者がお問い合わせAPIのレート制限を容易にバイパスし、スパムや大量のメール送信を引き起こす（OOM DoSおよび外部サービスへの悪影響）恐れがあります。
🔧 Fix: `auth.ts` と同様に、バケット上限に達した際は単に最古のキーを削除するのではなく、期限切れ（ウィンドウ外）のエントリをスキャンして優先的に削除する `evictOldestBucket` 関数を実装しました。
✅ Verification: `bun run test` を実行し、既存のテストスイートがすべて通過することを確認しました。また、パッチ適用前後でローカルのスクリプトを用いてバイパスが不可能になったことを検証済みです。
