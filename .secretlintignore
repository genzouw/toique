# Dummy secrets allowed in examples and docs
.env.example
compose.yml
# README.md contains dummy localhost PostgreSQL connection strings in setup examples
# (e.g. `postgresql://toique:toique@localhost:5433/toique`) that match the
# `database-connection-string` rule. These are non-secret placeholders that
# mirror compose.yml; excluding the whole file keeps the rule strict elsewhere.
README.md
# docs/ contains test fixtures and setup examples (e.g. `const secret = 'test-channel-secret'`)
# that trigger false positives. Individual files within docs/ are not excluded individually
# because there are too many to enumerate. Use secretlint-disable comments for specific lines
# if needed in the future.
docs/
# .github/workflows/ci.yml runs migrations/tests against the CI-local service-container
# Postgres and embeds `postgresql://toique:toique@localhost:5432/toique` literally in
# the `docker run -e DATABASE_URL=...` invocations. This is the same non-secret
# placeholder pattern used in compose.yml; excluding the file keeps the rule strict
# elsewhere while letting the workflow commit cleanly.
.github/workflows/ci.yml
