## 2023-10-24 - Form Input autocomplete
**Learning:** Proper `autoComplete` attributes on custom input components are critical for password manager compatibility and WCAG SC 1.3.5 compliance, reducing user friction significantly.
**Action:** Always expose and map the standard HTML `autoComplete` property on custom `<input>` wrappers like `AuthField`.
