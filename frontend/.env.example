# 公開サイトのオリジン URL（末尾スラッシュなし）。
# canonical / OGP / sitemap / robots.txt の生成に使用される。
# vite.config.ts (loadEnv) と src/lib/site.ts (import.meta.env) の2箇所から参照され、
# 値が未設定の場合は各箇所のフォールバック値（同一値）が使われる。
VITE_SITE_ORIGIN=https://example.com
