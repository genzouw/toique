# OSV-Scanner 設定ファイル
#
# 検出された脆弱性のうち、即時対応が難しい transitive dependency を期限付きで許容する。
# 期限到来後は再評価し、依存パッケージのアップデート可否を確認すること。
#
# 参考: https://google.github.io/osv-scanner/configuration/

# brace-expansion 2.0.1 (cdktf 経由) — ReDoS
[[IgnoredVulns]]
id = "GHSA-f886-m6hf-6m8v"
ignoreUntil = 2026-12-12
reason = "cdktf -> archiver の transitive dependency。直接の dependency 更新では解消できないため、cdktf 側の対応待ち。期限切れ時に再評価する。"

# brace-expansion 2.0.1 (cdktf 経由) — ReDoS
[[IgnoredVulns]]
id = "GHSA-v6h2-p8h4-qcjw"
ignoreUntil = 2026-12-12
reason = "cdktf -> archiver の transitive dependency。直接の dependency 更新では解消できないため、cdktf 側の対応待ち。期限切れ時に再評価する。"

# glob 10.4.5 (cdktf 経由) — ReDoS
[[IgnoredVulns]]
id = "GHSA-5j98-mcp5-4vw2"
ignoreUntil = 2026-12-12
reason = "cdktf -> archiver -> archiver-utils の transitive dependency。直接の dependency 更新では解消できないため、cdktf 側の対応待ち。期限切れ時に再評価する。"

# lodash 4.17.21 — Prototype Pollution
[[IgnoredVulns]]
id = "GHSA-f23m-r3pf-42rh"
ignoreUntil = 2026-12-12
reason = "cdktf -> archiver-utils の transitive dependency。lodash 4.x の修正版はリリースされていないため、cdktf 側の依存差し替え待ち。期限切れ時に再評価する。"

# lodash 4.17.21 — Command Injection
[[IgnoredVulns]]
id = "GHSA-r5fr-rjxr-66jc"
ignoreUntil = 2026-12-12
reason = "cdktf -> archiver-utils の transitive dependency。lodash 4.x の修正版はリリースされていないため、cdktf 側の依存差し替え待ち。期限切れ時に再評価する。"

# lodash 4.17.21 — ReDoS
[[IgnoredVulns]]
id = "GHSA-xxjr-mmjv-4gpg"
ignoreUntil = 2026-12-12
reason = "cdktf -> archiver-utils の transitive dependency。lodash 4.x の修正版はリリースされていないため、cdktf 側の依存差し替え待ち。期限切れ時に再評価する。"

# minimatch 5.1.6 / 9.0.5 — ReDoS
[[IgnoredVulns]]
id = "GHSA-23c5-xmqv-rm74"
ignoreUntil = 2026-12-12
reason = "cdktf -> archiver 系の transitive dependency。直接の dependency 更新では解消できないため、cdktf 側の対応待ち。期限切れ時に再評価する。"

# minimatch 5.1.6 / 9.0.5 — ReDoS
[[IgnoredVulns]]
id = "GHSA-3ppc-4f35-3m26"
ignoreUntil = 2026-12-12
reason = "cdktf -> archiver 系の transitive dependency。直接の dependency 更新では解消できないため、cdktf 側の対応待ち。期限切れ時に再評価する。"

# minimatch 5.1.6 / 9.0.5 — ReDoS
[[IgnoredVulns]]
id = "GHSA-7r86-cg39-jmmj"
ignoreUntil = 2026-12-12
reason = "cdktf -> archiver 系の transitive dependency。直接の dependency 更新では解消できないため、cdktf 側の対応待ち。期限切れ時に再評価する。"
